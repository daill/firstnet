use crate::net::{network::Network, layer::Layer};

mod net;

fn main() {
    println!("Hello, world!");
    let input_layer = Layer::new(2);
    let output_layer = Layer
    let network = Network::new();
}
